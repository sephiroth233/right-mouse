# 菜单管理界面设计验收

final result: passed

日期：2026-09-23。范围为本轮获批准的菜单管理界面，原生 macOS 27 / Apple Silicon；不代表全应用深色模式、VoiceOver 或跨系统矩阵已通过。

## 目录

- [比较基准](#比较基准)
- [迭代与结果](#迭代与结果)
- [交互验证](#交互验证)
- [参考](#参考)

## 比较基准

- 来源：[批准的效果图](docs/design/menu-editor-reference.png)，1490×1055 像素，生成图没有设备像素密度元数据约定。
- 实现：[原生截图](docs/design/menu-editor-actual.png)，CUA 捕获应用窗口，960×680 像素；浏览器 CSS viewport / deviceScaleFactor 不适用。
- 将来源缩放到 960×680（`.build/menu-editor-reference-960.png`），与实际截图在同一次图像输入中共同检查。两图均为浅色、菜单管理、全部筛选、新建文件展开、其他三类折叠、选中文件预览、五条一级操作和 RightMouse 子菜单。
- 验收使用隔离配置；Shell 隐藏，TXT/Markdown 置顶。实际截图 JSON 行有最近修改的浅蓝提示。系统标题栏、原生 Picker、SF Symbols 保留平台外观；不是像素级临摹。
- 正常用户配置另行启动检查，未被截图夹具覆盖。

## 迭代与结果

| 轮次 | 问题 | 修复与复核 |
| --- | --- | --- |
| 首轮 | P2：默认高度下底部分类未完整露出；长应用名称挤成两行；预览提示距离菜单过远 | 缩减标题及行间空白、侧栏改为 184 pt、预览分配 40% 内容宽度；预览滚动区按内容高度设上限 |
| 修复后 | 同状态重新捕获并与归一化来源共同检查 | 四类及底部操作均完整可见；VS Code 单行展示；提示紧接预览区；无内容覆盖、横向溢出或窗口被内容撑高 |

重点检查了导航选中状态、筛选与搜索同行布局、表头与位置控件对齐、图标/文字/隐藏状态对比、一级菜单长名称、底部排序入口和滚动边界。960×680 的全图中这些文字和控件清晰可辨，无需另做局部裁图。

有意保留的差异：连接状态写“已连接”，不把连接成功描述成 Finder 同步回执；文件操作实际为 4 项，使用真实目录而非效果图中的 3 项；采用现有品牌资产与系统控件。窗口保留系统标题栏与居中初始化策略。

最终未发现待修复的 P0/P1/P2 视觉问题。更小字号属于原生中等窗口密度选择，主要操作文字为 13 pt，说明为系统 caption，预览为 12.5 pt。

## 交互验证

- 隐藏 JSON 后重启仍保持隐藏；恢复子菜单后状态及预览更新。
- 搜索无结果显示空态，清除筛选恢复；已隐藏筛选只显示隐藏项目。
- 排序弹窗中下移 TXT 后位于 Markdown 后；拖动 TXT 行回到首位，主预览同步恢复。
- 预览逐层进入 RightMouse → 新建文件，只显示 JSON/YAML/HTML；隐藏 Shell 不显示。
- 切换空白处场景返回该场景根菜单。
- 真实 Finder 同配置显示五条一级操作，子菜单与预览一致，Shell 不存在。
- 从 DMG 更新安装副本后连接就绪，正常用户的一级顺序、6 个模板及 4 个应用保留。

搜索或筛选时自动展开匹配分类，便于直接定位；一般模式下可折叠。分类批量设置与恢复布局的状态语义由核心检查覆盖。

## 参考

- [菜单编辑契约](specs/001-finder-core/contracts/local-xpc.md)
- [施工与交付验收](specs/001-finder-core/evidence/EV-menu-editor-001.md)
